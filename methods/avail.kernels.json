{
  "name": "avail.kernels()",
  "params": {
    "isXen": {
      "type": "boolean",
      "required": false,
      "description": "Show or hide Xen compatible kernels"
    },
    "isKVM": {
      "type": "boolean",
      "required": false,
      "description": "Show or hide KVM compatible kernels"
    }
  }
}